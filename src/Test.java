/**
 * @author Drumstyle92
 * class that contains the main.
 */
public class Test {
    /**
     * @param args main parameter.
     * main method that contains an object and a method that shows multiplications.
     */
    public static void main(String[] args) {
        CustomNumber number = new CustomNumber();
        System.out.println("--------multiplicationTable---------");
        number.getMultiplicationTable();
    }
}
